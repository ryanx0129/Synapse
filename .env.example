# Server-side only. Never prefix this with VITE_.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6
OPENAI_TIMEOUT_MS=20000

# Frontend test/demo controls. These values never contain secrets.
VITE_DETERMINISTIC_MODE=false
VITE_SIMULATE_API_FAILURE=false
